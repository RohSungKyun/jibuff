All commands exposed by the `jb` / `jibuff` CLI. The two primary workflow commands are `interview` (requirement clarification) and `run` (agent loop). The remaining commands support operations: observability (status, inspect), health checks (doctor), workspace maintenance (cleanup, recover), and integration setup (setup for MCP registration in Claude Code, setup-skill for Codex, mcp serve to run the stdio server).
