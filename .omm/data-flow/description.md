Tracks how data transforms as it moves through jibuff. User text → interview session state (MCP path) → spec/tasks.md. Tasks.md + task_status.json together define queue state. Per-task: failure context from last_failure.md + task description build the agent prompt; agent output feeds validators; validator errors write last_failure.md; passing output optionally feeds the quality evaluator. All outcomes write per-iteration traces.
