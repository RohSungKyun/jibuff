Jibuff is a 6-layer spec-driven workflow harness for AI coding agents. It absorbs requirement ambiguity before the agent touches code: the CLI/MCP entry points route user requests through an Interview layer that scores ambiguity and risk, produces a task list, and then hands off to the Orchestrator which drives agents through a run→validate→quality loop, writing structured artifacts to Storage throughout.
