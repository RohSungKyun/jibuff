interview/engine.py. Core interview orchestrator. start() creates an InterviewSession. step() advances one round: Stage 1 keyword coverage (free, no LLM call), then Stage 2+3 in async parallel (contradiction detection, dimensional scoring, risk scoring via OpenRouter). Generates QuestionBlocks with a/b/c choices. generate_tasks_md() produces spec/tasks.md from completed session transcript.
