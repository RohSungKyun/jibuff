Core loop layer in orchestrator/. Three collaborating classes: LoopController drives the task cycle (run→validate→ralph→pass/fail, escalation, auto-commit), AgentRunner spawns the coding agent CLI subprocess per task with a built prompt, and TaskQueue parses spec/tasks.md and persists status in storage/task_status.json. Config and RuntimeStore provide mode config and distributed heartbeat tracking respectively.
